name: Flutter CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'
          channel: 'stable'

      - name: Clean and get dependencies
        run: |
          rm -rf .dart_tool pubspec.lock
          flutter clean
          flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings
        continue-on-error: true

      - name: Run tests
        run: flutter test
        continue-on-error: true

  build-ios:
    name: Build iOS (unsigned)
    runs-on: macos-latest
    needs: test
    # macOSランナーはLinuxの10倍のAction分数を消費するため、通常のpushでは実行しない。
    # PR時、または手動実行（Actionsタブ → Run workflow）でのみ起動する。
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'
          channel: 'stable'

      # google_mobile_ads(CocoaPods)とwebview_flutter_wkwebview(SPM)の
      # 混在でpod installが失敗するため、SPM統合を無効化してCocoaPods統一にする
      - name: Disable Swift Package Manager
        run: flutter config --no-enable-swift-package-manager

      - name: Clean and get dependencies
        run: |
          rm -rf .dart_tool pubspec.lock
          flutter clean
          flutter pub get

      # コード署名なしのビルド（コンパイルが通るかの確認）のみを行う軽量ジョブ。
      # 署名付きビルド・TestFlightアップロードは build-ios-signed ジョブ（手動実行）を使う。
      - name: Build iOS (no codesign)
        run: flutter build ios --release --simulator --no-codesign

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: Runner.app
          path: build/ios/iphoneos/Runner.app

  build-ios-signed:
    name: Build & Upload Signed iOS (TestFlight)
    runs-on: macos-latest
    needs: test
    # 最もコストの高いジョブ（署名・アップロードまで含む）。誤って毎pushで走らないよう
    # 手動実行（Actionsタブ → Run workflow）でのみ起動する。
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'
          channel: 'stable'

      # google_mobile_ads(CocoaPods)とwebview_flutter_wkwebview(SPM)の
      # 混在でpod installが失敗するため、SPM統合を無効化してCocoaPods統一にする
      - name: Disable Swift Package Manager
        run: flutter config --no-enable-swift-package-manager

      - name: Clean and get dependencies
        run: |
          rm -rf .dart_tool pubspec.lock
          flutter clean
          flutter pub get

      - name: Import signing certificate
        env:
          P12_BASE64: ${{ secrets.IOS_DIST_CERT_BASE64 }}
          P12_PASSWORD: ${{ secrets.IOS_DIST_CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: temporary-ci-keychain-password
        run: |
          echo "$P12_BASE64" | base64 --decode > certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import certificate.p12 -k build.keychain -P "$P12_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          security list-keychains -d user -s build.keychain login.keychain
          rm certificate.p12

      - name: Install provisioning profile
        env:
          PROFILE_BASE64: ${{ secrets.IOS_PROVISION_PROFILE_BASE64 }}
        run: |
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$PROFILE_BASE64" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision

      - name: Install App Store Connect API key
        env:
          API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
          KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
        run: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$API_KEY_BASE64" | base64 --decode > ~/.appstoreconnect/private_keys/AuthKey_${KEY_ID}.p8

      - name: Build signed IPA
        run: flutter build ipa --release --export-options-plist=ios/ExportOptions.plist

      - name: Upload to App Store Connect (TestFlight)
        env:
          KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          xcrun altool --upload-app -f build/ios/ipa/*.ipa -t ios \
            --apiKey "$KEY_ID" --apiIssuer "$ISSUER_ID"
