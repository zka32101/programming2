# 算数コレ！ — 改善タスク一覧

**最終更新**: 2026年6月8日  
**バージョン**: 2.0.0+

---

## 🐛 バグ修正 (優先度: 高)

### 1. 選択肢のランダム配置
**現状**: 正解が2番目または3番目に偏っている  
**対応**: ランダム配置に修正  
**ファイル**: `lib/data/stage_data.dart` - `QuizQuestion` の `choices` と `correctIndex` をシャッフル  
**影響度**: 中 - ユーザー体験向上
```dart
// 改善前
choices: ['1', '2', '3', '4'],
correctIndex: 1,  // 常に2番目

// 改善後
// buildTimeにシャッフル処理を追加
List<String> shuffledChoices = [...choices]..shuffle();
int newCorrectIndex = shuffledChoices.indexOf(choices[correctIndex]);
```

---

### 2. 紹介機能のバグ修正（緊急）
**現状**: 紹介画面から戻るたびにコイン報酬が付与される  
**対応**: 以下に修正
- 紹介キーを発行（一意なコード）
- 紹介された側がコード入力時に紹介した側にコイン追加
- 一度の共有で最大5件まで

**ファイル**:
- `lib/models/referral_model.dart` - 紹介コード管理モデル作成
- `lib/providers/referral_provider.dart` - 紹介ロジック
- `lib/screens/referral_screen.dart` - UI修正

**詳細フロー**:
```
紹介する側:
1. 「友達に紹介」ボタン → ユニークキー生成（例: "SK2026ABC123"）
2. 共有ボタン → LINEやメールで相手にキーを送信
3. キーが入力されるたびにコイン加算（最大5回まで）

紹介された側:
1. アプリDL
2. チュートリアル後「友達コードを入力」
3. コード入力 → 両方にコイン追加
   - 紹介した側: +100コイン
   - 紹介された側: +50コイン
```

---

### 3. ユーザー切り替え時の情報残存バグ
**現状**: 人を変えても前の人の情報が保持される  
**対応**: ログアウト時に完全にデータをクリアする  
**ファイル**:
- `lib/providers/profile_provider.dart`
- `lib/providers/progress_provider.dart`
- `lib/screens/login_screen.dart`

**修正内容**:
```dart
// ログアウト処理
Future<void> logout() async {
  // ローカルデータをクリア
  await SharedPreferences.getInstance().then((prefs) {
    prefs.clear();  // すべてクリア
  });
  
  // Firebaseからログアウト
  await FirebaseAuth.instance.signOut();
  
  // Provider をリセット
  ref.invalidate(profileProvider);
  ref.invalidate(progressProvider);
  ref.invalidate(coinProvider);
  ref.invalidate(badgeProvider);
  ref.invalidate(characterProvider);
}
```

---

## ✨ 機能改善 (優先度: 中)

### 4. ホーム画面に解説セクション追加
**現状**: 問題を解くだけ  
**対応**: ホーム画面に「算数のやり方」解説コーナーを追加

**新規画面**: `lib/screens/math_guide_screen.dart`

**コンテンツ例**:
```
【1年生】
- たし算のやり方（指を使った数え方）
- ひき算の考え方
- 数字の読み方・書き方

【2年生】
- かけ算の仕組み
- かけ算の九九
- 割り算とは？

【3年生】
- 分数（1/2、1/3とは？）
- 時間の読み方
- 図形の基本

【4年生以上】
- 小数の計算
- 面積と体積
- グラフと統計
- 関数の導入
```

**UI構成**:
- ホーム画面に「？ 算数ガイド」セクション
- タップするとカテゴリ別に詳細解説
- 動画または図解で「わかりやすく」説明

---

### 5. ステージ数を2倍に拡張
**現状**: 各学年 9ステージ（総54ステージ）  
**対応**: 各学年 18ステージに（総108ステージ）

**方針**:
- 難易度を細分化
- 得意な子は進める、苦手な子は反復学習
- 各ステージ 3～5問に絞る（短時間でクリア感を）

**例: 1年生のステージ構成**:
```
1. たし算 1 (1+1～2+2)
2. たし算 2 (3+1～3+3)
3. たし算 3 (4+1～4+4)
4. たし算 4 (5+1～5+5)
5. たし算 ミックス (おさらい)
6. ひき算 1 (2-1～3-1)
7. ひき算 2 (3-2～4-2)
8. ひき算 3 (4-1～5-2)
... 計18ステージ
```

**作業**:
- `lib/data/stage_data.dart` を大幅拡張
- 問題数: 312問 → 600問以上に
- 各ステージのバランス調整

---

### 6. コンテンツ不足箇所を補充
**確認項目**:
- [ ] 文章題（特に3年生以上）
- [ ] 図形問題（面積・周囲の長さなど）
- [ ] 時間・時計の読み方
- [ ] お金の計算（買い物）
- [ ] 統計・グラフ（4年生～）
- [ ] 分数・小数の計算
- [ ] 比例・反比例（6年生向け）

**追加方針**:
- 各学年で5～10問程度、実生活に即した問題を追加
- 子どもが「何に使うの？」と感じる内容を意識

---

### 7. 漢字にふりがなを追加
**対象**: 問題文・解説・UI内のすべての漢字  
**方法**: `ruby` タグで実装（Flutterの場合は TextSpan を使用）

**例**:
```
修正前:
「6個のりんごと3個のみかんがあります。全部でいくつでしょう?」

修正後:
「6個(こ)のりんごと3個(こ)のみかんがあります。全部(ぜんぶ)でいくつでしょう?」
```

**実装例**（Flutter）:
```dart
RichText(
  text: TextSpan(children: [
    TextSpan(text: '6個', style: boldStyle),
    TextSpan(text: 'こ', style: rubyStyle),  // 小さいテキスト
    TextSpan(text: 'のりんごがあります'),
  ]),
)
```

**優先度**: 
- 1年生: すべての漢字（ひらがなでもOK）
- 2年生: 教科書の「習う漢字」にふりがな
- 3年生以上: 難しい漢字のみ

---

## 📅 実装スケジュール案

### Phase 1（Week 1-2）- 緊急バグ修正
- [ ] 紹介機能バグ修正
- [ ] ユーザー切り替えバグ修正
- [x] テスト・デプロイ

### Phase 2（Week 2-3）- 機能追加
- [ ] 選択肢ランダム配置
- [ ] ホーム画面 ガイドセクション追加
- [ ] ふりがな対応（1年生から）

### Phase 3（Week 4-6）- 大規模拡張
- [ ] ステージ倍増（54 → 108）
- [ ] コンテンツ追加（312 → 600問以上）
- [ ] 全テスト・品質保証

### Phase 4（Week 7+）- ポーランド・リリース
- [ ] ビルド・署名
- [ ] Google Play 提出
- [ ] v2.1 計画

---

## 🎯 優先度マトリックス

| タスク | 優先度 | 工数 | 影響度 | 実装者 |
|--------|--------|------|--------|--------|
| 紹介バグ修正 | 🔴 高 | 中 | 高 | Flutter Dev |
| ユーザー切り替えバグ | 🔴 高 | 小 | 高 | Flutter Dev |
| 選択肢ランダム配置 | 🟡 中 | 小 | 中 | Flutter Dev |
| ふりがな対応 | 🟡 中 | 大 | 中 | Designer + Dev |
| ガイド画面追加 | 🟡 中 | 大 | 中 | Designer + Dev |
| ステージ倍増 | 🟡 中 | 大 | 高 | Content Creator |
| コンテンツ追加 | 🟢 低 | 中 | 中 | Content Creator |

---

## 📝 Notes

- **紹介キー形式**: `"SANSU" + 年月日 + ランダム5文字`
  - 例: `SANSU20260608ABCDE`
  - 管理: Firebase Firestore に紹介コード テーブル作成

- **ふりがな**: Flutterの `RichText` + `TextSpan` で実装可能
  - または、Web版は HTML の `<ruby>` タグ使用

- **ステージ倍増の品質**: 各ステージを短く（3～5問）して達成感を高める

- **ローカライズ**: 将来的に英語対応も想定し、翻訳可能な設計に

---

**最終確認**: v2.0.0のv2.1アップデートで上記の改善を実装予定

